import { Formulario, Title, ContainerTitle } from './styles';
import { Container } from '../../global';

const Hero = () => (
  <Formulario>
    <Container>
      <ContainerTitle>
        <Title>
          As melhores vagas para tecnologia, design e artes visuais.
        </Title>
      </ContainerTitle>
    </Container>
  </Formulario>
);

export default Hero;
